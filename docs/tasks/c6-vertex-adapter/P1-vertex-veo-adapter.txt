TASK: Correction 6 / P1 — Vertex Veo adapter

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.

VERSIONING
- Required unchanged project version: `0.6.5`.

GOAL

Add Vertex AI Veo as a separate Google video-generation adapter behind the existing provider-neutral cinematic and simple presenter-video contracts. Preserve the current Gemini Developer API Veo adapter and all current Phase 6 behavior.

Read BOOT.md, docs/integrations/google-video.md, the current c6 correction prompts/implementation, neutral video contracts, Developer API Veo adapter, workflows, generated-media provenance/fingerprinting, and focused tests before editing.

MUST REMAIN TRUE

- The existing Google Developer API Veo adapter remains behaviorally unchanged and independently usable.
- The Vertex adapter implements the existing `VideoGenerationClient` and `PresenterVideoGenerationClient` contracts; do not create a second creative contract or leak Vertex request/response shapes upstream.
- Vertex runtime configuration is separate from Developer API credentials. Use:
  - `GOOGLE_CLOUD_PROJECT`;
  - `GOOGLE_CLOUD_LOCATION`, initially only `us-central1`;
  - `VIDGEN_VERTEX_VIDEO_MODEL`.
- Initially support the GA reference-capable models `veo-3.1-generate-001` and `veo-3.1-fast-generate-001`. Fail configuration for Lite, preview, unknown, or path-shaped model values rather than silently dropping required capabilities.
- Use supported Application Default Credentials for Vertex bearer authentication. Add only the smallest official ADC-capable dependency if the repository has none; do not create a credential-file parser, shell out to `gcloud`, add a broad Google SDK stack, or create a package lock.
- Use the regional Vertex REST surface:
  `https://us-central1-aiplatform.googleapis.com/v1/projects/<project>/locations/us-central1/publishers/google/models/<model>:predictLongRunning`
  and poll with `:fetchPredictOperation` using the returned full operation name.
- Keep provider identity distinguishable in neutral provenance, e.g. a stable Vertex-specific provider value, while model and safe operation identifiers remain available through existing result fields.
- Keep the current portrait/720p/one-sample Veo generation semantics. Reference-image generation remains an 8-second initial operation and extension remains 7 seconds.
- Current Vertex reference-image input supports PNG/JPEG asset references. Reject an otherwise-approved WebP reference before auth/network activity rather than transcoding, omitting it, or weakening the existing shared local-reference boundary.
- Keep reference images local/in-memory and send them as documented base64 asset references. Do not add publisher retrieval or upload anchor references to Cloud Storage.
- For this correction, use the documented inline video result path by omitting `storageUri`; accept only bounded inline MP4 output. Cloud Storage staging remains unimplemented unless current provider evidence proves inline output cannot satisfy the required operation.
- If an extension is needed, use the documented video input shape with the just-produced prior MP4 and preserve the existing engine-owned duration/extension plan. Do not invent a different timing model for Vertex.
- Preserve whichever completed simple presenter speech-partition/timing semantics exist in the execution checkout, including the c6 9-15 second retained-window behavior. Do not reintroduce raw-15-second dialogue allocation or change cinematic presenter partitioning merely to share code.
- If prompt/chunk/request helpers are extracted for reuse, the Developer API adapter's observable request behavior and tests must remain unchanged.
- Treat ADC tokens and all Vertex responses as untrusted/secret-bearing runtime data. Never persist or surface tokens, raw responses, credential paths, or authorization headers.
- Bound token/auth failure, HTTP requests, polling, response bytes, base64 decoding, generated bytes, timeouts, and operation counts. Reject malformed operations, provider errors, filtered/no-video results, non-MP4 output, oversized output, invalid signatures, and incompatible duration/provenance safely.
- No automatic retry/fallback to the Developer API backend.
- No changes to StoryInput, ClipPlan, templates, FFmpeg, sidecar schema, generated-media schema, CLI spelling, ngest, or Phase 7 behavior.
- Package and engine version remain 0.6.5.

PROOF

Focused tests must prove:
- Vertex project/location/model validation fails before auth/network for unsafe or unsupported configuration;
- ADC/token acquisition is injected/mockable in tests and the production request uses Bearer auth without `GEMINI_API_KEY`;
- request URL/body use the configured project/model, `us-central1`, 9:16, 720p, one sample, and the expected 8-second reference-image initial request;
- one to three PNG/JPEG asset references are encoded exactly; WebP fails before auth/network;
- operation polling uses `:fetchPredictOperation` with the exact returned operation name;
- bounded inline MP4 response decoding succeeds only for one valid result and rejects malformed, filtered, empty, oversized, wrong-MIME, or invalid-signature output;
- simple 4-8 second requests use one operation; simple 9-15 second requests use exactly one extension and preserve the current exact-dialogue timing contract;
- cinematic content/presenter generation preserves current neutral duration and extension semantics;
- extension requests consume the immediately prior MP4 and return ordered operation provenance;
- auth, timeout, HTTP, malformed JSON/operation, and provider failures expose only safe VidGen errors;
- the existing Developer API Veo focused tests remain green without changed expectations.

Run focused Vertex/Developer-Veo/presenter/generated-media tests, `npm run check`, and `git diff --check`.

Downstream-required capability -> owning implementation/export -> focused proof:

Vertex authenticated generation -> Vertex video adapter implementing existing neutral clients -> focused mocked REST/auth tests.

approved local presenter references -> Vertex documented asset-reference request -> reference validation/request-shape tests.

Vertex operation result -> existing neutral raw-media/provenance result -> bounded response/extension tests.

NON-GOALS

Do not add backend selection/wire workflows yet; P2 owns that. Do not add GCS staging, Flow automation, new creative stages, publisher retrieval, fan-out, databases/queues, publishing, or generalized provider architecture.

FINAL RESPONSE

Report the adapter boundary, Vertex runtime/auth contract, supported models/reference constraints, inline-result/extension behavior, dependency change if any, exact validation run, Developer API regression status, and confirmation that 0.6.5 is unchanged.
