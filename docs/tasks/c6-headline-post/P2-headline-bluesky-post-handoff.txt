TASK: Correction 6 / P2 — Headline Bluesky post handoff

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.

VERSIONING
- Required unchanged project version: `0.6.5`.

GOAL

Add a fresh VidGen CLI command that proves the simple headline production chain can hand a completed clip to the independently installed VidGen Poster and publish it to Bluesky, while keeping all publishing implementation owned by jfin602/vidgen-poster.

Start from the P1 dry-run/preflight result. Inspect the current VidGen CLI/headline result contract and the current VidGen Poster CLI contract before editing. Do not import Poster source code or reimplement its publishing behavior.

MUST REMAIN TRUE

- Add `vidgen headline-post` with the same headline generation inputs as `vidgen headline`, including `--dry-run`.
- The Poster installation root is runtime configuration via `VIDGEN_POSTER_ROOT`; do not hardcode a deployment path.
- VidGen invokes VidGen Poster only as an external process from the configured Poster repository/runtime. Use argument-vector execution with no shell interpolation. Headline/source text, paths, and configuration are untrusted values and must remain single arguments.
- Do not add Bluesky SDK dependencies, credentials, upload logic, duplicate logic, receipts, or platform response parsing to VidGen. Poster remains the publication authority.
- Before spending on text/Veo generation, normal `headline-post` must complete VidGen local headline preflight and a successful Poster `doctor bluesky` readiness check. Missing/invalid Poster configuration or a failed doctor must prevent generation.
- After readiness succeeds, run the existing headline generation path. Poster post invocation may occur only after the finished MP4 and its coupled sidecar have reached the existing successful headline publication boundary.
- The Bluesky caption is exactly `"<headline>" by <source display name>`, using the same governed Article/source identity that produced the clip. Do not summarize, rewrite, add hashtags, or ask a model to create publishing text.
- The real handoff is the equivalent of Poster `post bluesky --video <finished-mp4> --text <caption>`. Do not pass Poster `--dry-run` on a real `headline-post` run.
- `headline-post --dry-run` performs VidGen headline preflight plus Poster `doctor bluesky` only. It must not call text/Veo generation, create a final clip, invoke Poster `post`, upload media, or create an external post.
- If headline generation fails, Poster post must never run. If Poster publishing fails after generation, preserve the completed VidGen MP4/sidecar, return nonzero, and report a safe publishing-stage failure rather than deleting valid generated artifacts.
- Do not forward or persist raw child-process stderr/stdout as trusted diagnostics. No secrets, authorization data, app passwords, raw provider responses, or shell command strings may enter VidGen artifacts or public errors.
- Do not bypass Poster duplicate protection or add a VidGen `--allow-duplicate` path in this correction.
- Existing `headline`, its new P1 dry-run behavior, and all cinematic commands remain unchanged.
- Package and engine version remain 0.6.5.

PROOF

Focused tests must cover:
- `headline-post` parsing/help for normal and dry-run forms;
- missing/invalid Poster root or failed `doctor bluesky` stops before generation;
- local headline preflight occurs before Poster doctor and generation;
- dry run reaches Poster doctor but never generation or Poster post;
- normal success invokes Poster post only after headline final readiness;
- exact caption construction, including headlines containing whitespace/quotes or shell-significant characters, reaches Poster as one `--text` argument with no shell interpretation;
- generation failure never invokes Poster post;
- Poster failure after generation leaves the completed headline MP4/sidecar intact and returns a safe failure;
- no publishing credentials or raw child output are persisted or exposed;
- existing headline and cinematic CLI regression coverage remains green.

Run focused CLI/headline/handoff tests, `npm run check`, `node src/cli.ts --help`, and `git diff --check`.

Downstream-required capability -> owning implementation/export -> focused proof:

safe local headline readiness -> P1 shared preflight -> ordering/no-generation tests.

finished headline MP4 + governed Article/source identity -> existing headline workflow result/sidecar boundary -> handoff sequencing and exact-caption tests.

Bluesky readiness/publish/duplicates/receipts -> VidGen Poster external CLI -> child-process contract tests with no Poster logic duplicated in VidGen.

NON-GOALS

Do not modify jfin602/vidgen-poster, add Meta/X fan-out, scheduling, multi-platform orchestration, generated captions, allow-duplicate controls, database/queue work, live ngest fan-out, cinematic behavior, provider behavior, or roadmap/documentation files.

FINAL RESPONSE

Report the new CLI shape, Poster configuration boundary, exact caption rule, dry-run behavior, sequencing/failure semantics, exact validation run, and confirmation that Poster still owns publishing and version 0.6.5 remains unchanged.
