TASK: Correction 6 / P4 — Queue hardening closeout

- Recommended configuration: `Terra High`.
- Required unchanged project version: `0.6.5`.

Closeout objective

Review c6-queue-hardening as a follow-on safety and lifecycle correction after c6-runner-queue. Do not assume prerequisite behavior or task completion merely because tests pass.

Required review

- Inspect the complete post-c6-runner-queue baseline and P1-P3 diff.
- Verify ngest is used only as a coherent discovery snapshot boundary and that discovery compares governed articleIds against durable ever-seen history.
- Verify failed/drifting snapshots cannot partially advance discovery, reappearing seen IDs are not new, and pending evaluation survives feed removal/restart.
- Verify no client-side feed truncation was introduced; ngest remains owner of the active feed/result limit.
- Verify `VIDGEN_WORKER_QUEUE_EXPIRATION_DAYS` defaults to exactly 3 days, validates safely, uses durable queue/admission time, and is a binary eligibility cutoff rather than score mutation.
- Verify expired work persists safely but spends no generation attempt/daily slot and cannot block eligible work.
- Verify the default daily generation limit remains exactly one start per UTC day.
- Verify generation-limit exhaustion pauses generation only: polling, discovery, evaluation, admission, queue insertion, and score reordering continue.
- Verify the first normal poll after UTC rollover selects the highest-scored still-unexpired eligible candidate, subject to prior-production dedupe and the global single-generation guard.
- Verify generate mode never publishes and live publication reuses successful media, including partial publication retries.
- Re-verify c6-runner-queue invariants: score-priority ordering, valid existing-production adoption, no duplicate generation by articleId, and <=1 active Worker-managed generation subprocess even with duplicate Worker processes.
- Confirm package version remains exactly 0.6.5 and package-lock.json is absent.

Validation

Run focused discovery/config/queue tests, npm run test:worker, npm run test:app, npm run check, and git diff --check.

Perform deterministic local evidence without live provider spend:
- coherent snapshot change/reappearance scenario;
- queue growth after daily slot exhaustion;
- three-day default expiration and alternate configured expiration;
- UTC rollover selecting the highest-scored eligible item;
- process-level generation-lock smoke inherited from c6-runner-queue.

Evidence boundaries

Do not make live Parallel, Veo, render, or social-platform calls solely for closeout. Clearly separate mocked/local lifecycle proof from previously observed or future live qualification.

Closeout result

Return GREEN only if atomic discovery, durable evaluation backlog, configurable expiration, one-per-UTC-day default generation behavior, priority selection after rollover, and inherited duplicate/concurrency protections are all demonstrated. Otherwise return NOT GREEN with the smallest bounded repair plan. Do not advance the roadmap phase or change package.json.
