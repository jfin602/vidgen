# Superseded correction stack

The executable `c6-lower-third-layout` stack is superseded.

Its original P1 was already executed and pushed as commit `2b5f054abd2b52b3707b814a002cd3a2ca26773e` before later prompt edits changed the requested color/prompt-file scope. Current main therefore contains the landed safe-area/font-measurement work, but not all later requirements.

Remaining work was re-planned against the pushed implementation under:

`docs/tasks/c6-prompt-file/`

Do not run `c6-lower-third-layout` again.
