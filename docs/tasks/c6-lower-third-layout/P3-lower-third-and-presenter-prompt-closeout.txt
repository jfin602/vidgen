TASK: Correction 6 / P3 — Lower-third and presenter-prompt closeout

MODEL / REASONING / USAGE
- Recommended configuration: `Terra Medium`.

VERSIONING
- Required unchanged project version: `0.6.5`.

GOAL

Review P1 and P2 as one bounded correction stack. Inspect the actual diff, lower-third fit/color policy, external prompt-asset boundary, prompt assembly, provenance/reuse identity, schema changes, workflow ordering, FFmpeg/Veo behavior, and focused tests. Do not approve merely because tests pass.

P1 ACCEPTANCE — LOWER THIRD

Confirm:
- the complete governed headline and source display name are preserved; no truncation, ellipsis, rewriting, shortening, or model authority over display text was introduced;
- automatic headline wrapping still preserves the prior `c6-line-wrap` whitespace semantics;
- the lower third has one explicit outer panel and an inner content safe area with positive left/right/top/bottom padding;
- the outer panel is exactly opaque RGB `#336699`, not the old black/translucent-black treatment;
- accepted headline/source glyph layout is deterministically constrained to the inner safe area rather than relying only on character count;
- the Venice/Lee Chang-dong regression no longer depends on the visually unsafe 42-character-line assumption;
- genuinely unfit display text fails before paid text/Veo generation;
- source placement remains inside the panel with bottom/right padding and clear separation from headline text;
- staged text-file/no-shell safety remains intact and untrusted display text does not enter the FFmpeg filter expression;
- FFmpeg output remains 1080x1920, 30 fps, H.264/yuv420p + AAC stereo 48 kHz with existing loudnorm/duration behavior and one simple-path input;
- finishing provenance and schema validation consistently use `simple-clip-finishing-policy-v2`.

P2 ACCEPTANCE — EXTERNAL PRESENTER PROMPTS

Confirm:
- `VIDGEN_VEO_SIMPLE_PRESENTER_PROMPT_FILE` and `VIDGEN_VEO_CINEMATIC_PRESENTER_PROMPT_FILE` are the only new prompt-file runtime settings;
- code does not hardcode the production `/home/deploy/vidgen-assets` path;
- each relevant presenter path loads one bounded regular local UTF-8 prompt asset and safely rejects missing/invalid input before Veo auth/network;
- operator prompt text is literal style guidance, not a template language and not authority over dialogue/story/provider/runtime fields;
- the exact bytes used to build the request are the bytes represented by prompt identity; no TOCTOU mismatch exists between request, fingerprint, and provenance;
- initial presenter requests include the engine-owned statement `The supplied reference images are AI-generated presenter images and do not depict a real person or celebrity.`;
- hardcoded presenter scaffolding no longer says `news-presenter` or `visual/news context`, while operator/story text is not rewritten;
- B-roll/content-video prompt behavior is unchanged;
- exact assigned dialogue, story grounding, extension timing/continuity, no-extra-dialogue, no-readable-onscreen-text, and no-unsupported-facts constraints remain engine-owned and regression-covered;
- extension prompts do not falsely claim that reference images are supplied on extension calls;
- simple headline sidecars record only safe prompt basename/SHA-256/byte-size identity under an honestly bumped sidecar schema version;
- cinematic generated-media provenance records the corresponding prompt identity under an honestly bumped generated-media schema version;
- presenter effective-generation fingerprints/reuse include the cinematic prompt identity so changing the file cannot reuse stale presenter media;
- unrelated content-video/voiceover reuse remains stable;
- prompt text, absolute prompt paths, credentials, headers, raw provider errors, and secrets are absent from artifacts/public errors;
- Agent Platform model/auth/endpoints/polling/reference encoding/inline MP4 behavior and duration semantics are unchanged.

VALIDATION

Run:
- `npm run codex:phase:validate -- c6-lower-third-layout`;
- focused simple-clip-finisher, headline-workflow/sidecar, Agent Platform Veo, media-workflow/generated-media/reuse tests;
- `npm test`;
- `npm run check`;
- `node src/cli.ts --help`;
- `git diff --check`.

Inspect tests specifically for full-text preservation, exact `#336699` panel treatment, safe-area geometry, font-aware/bounded fit, pre-provider rejection, hostile display-text staging, external prompt literal loading, AI-generated-reference statement, hardcoded presenter wording, exact dialogue preservation, prompt identity/schema provenance, prompt-sensitive presenter reuse, unchanged B-roll behavior, and unchanged provider request/auth/polling semantics.

LIVE EVIDENCE

Treat the owner's existing screenshot/playback as root-cause evidence only for the old lower-third treatment. Treat the observed Veo safety rejection of the current anchor images as live provider evidence only for that prior request; do not claim the new prompt wording changes provider safety classification.

No new live provider call is required for Codex closeout. After human review:
1. the owner can rerender the existing local Veo MP4 at zero provider cost to visually confirm the `#336699` lower third and padding;
2. the owner will create/update the configured prompt files under `/home/deploy/vidgen-assets/prompts/`;
3. only then should the next paid end-to-end presenter smoke be used to qualify the new prompt behavior.

BOUNDED REPAIRS

Repair only defects directly within lower-third fit/color, pre-provider display validation, finishing-policy provenance, external presenter-prompt loading/assembly, prompt identity/schema provenance, presenter reuse invalidation, focused docs, and regression coverage. Do not broaden into TTS, B-roll externalization, model/auth changes, provider-safety workarounds, Phase 7, or publishing.

FINAL RESPONSE

Report review findings, any bounded repair, exact validation, `#336699` safe-area semantics, governed-text preservation, external prompt-file contract, AI-generated-reference statement, removal of hardcoded presenter `news` wording, prompt provenance/reuse behavior, schema versions, unchanged provider behavior, unchanged 0.6.5 state, and whether `c6-lower-third-layout` is ready for human review.
