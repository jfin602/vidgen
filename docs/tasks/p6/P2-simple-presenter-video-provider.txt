TASK: Phase 6 / P2 — Simple presenter video provider

MODEL / REASONING / USAGE
- Recommended configuration: `Terra High`.

VERSIONING
- This prompt's assigned project version is `0.6.2`.

GOAL

Add provider-neutral single-presenter video generation for the Phase 6 path without routing it through cinematic GeneratedMediaUnit semantics.

Read BOOT.md, P1, the current generated-media/media-workflow code, Google Veo adapter, reference-image handling, and focused tests before editing. Reuse current safety code rather than cloning provider transport.

MUST REMAIN TRUE

- Introduce a narrow presenter-video capability with exact spoken text, approved local reference images, safe provenance, and a provider duration plan for a supplied 4-20 second ceiling.
- Keep the existing cinematic `VideoGenerationClient.generateVideo` contract and behavior compatible.
- The Google adapter should share its existing polling/download/credential-safety implementation; do not create a second Veo HTTP stack.
- Reuse/extract the current approved anchor-reference validation so simple and cinematic paths do not diverge on MIME, byte limits, hashes, or one-to-three reference rules.
- Current Veo reference-image behavior must remain hidden behind the provider capability. Verify current official Veo documentation before editing: https://ai.google.dev/gemini-api/docs/veo
- With the current reference-image Veo capability, support the Phase 6 ceiling honestly: raw provider coverage may exceed a sub-8-second final ceiling and be deterministically trimmed later; do not claim raw provider duration equals final duration.
- Do not use a 22-second generation for this <=20-second MVP path. Use the existing 8-second initial generation and at most one 7-second extension.
- Presenter prompting must require only the assigned dialogue, no added dialogue, no readable/generated on-screen text, and no unsupported story facts.
- Preserve bounded polling, redirects, download size/type/signature checks, operation provenance, secret-safe errors, and existing cinematic extension behavior.
- No FFmpeg, final artifacts, CLI, live fan-out, publisher retrieval, or new dependency.

PROOF

Add focused provider/reference tests for duration planning across the 4-20 range, 8-second reference-image generation, one-extension coverage, exact dialogue, no cinematic request dependency, and unchanged cinematic Veo behavior. Run focused provider/media tests, `npm run check`, and `git diff --check`.

Downstream-required capability -> owning implementation/export -> focused proof:

simple presenter duration plan + raw generated bytes/provenance -> provider-neutral presenter-video capability implemented by Google Veo -> focused Veo/provider tests.

FINAL RESPONSE

Report the duration mapping actually implemented, reuse of existing Veo/reference safety, tests run, and confirmation the cinematic provider surface did not regress.
